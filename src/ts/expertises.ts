import { ExpertiseSubPageType } from './expertise-page';
import { SectionType } from './section';

export type ExpertiseEngineerType = {
  title: string;
  intro: string;
  sections: SectionType[];
  expertises: ExpertiseSubPageType[];
};
export type ExpertiseDesignType = {
  title: string;
  intro: string;
  sections: SectionType[];
  expertises: ExpertiseSubPageType[];
};
export type ExpertiseDigitalType = {
  title: string;
  intro: string;
  sections: SectionType[];
  expertises: ExpertiseSubPageType[];
};
export type ExpertiseFormationType = {
  title: string;
  intro: string;
  sections: SectionType[];
  expertises: ExpertiseSubPageType[];
};
